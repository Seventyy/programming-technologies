<UserControl x:Class="PresentationLayer.View.CustomerListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PresentationLayer.View"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="800">
    <Grid>
        <Label Content="Testing..." Margin="10,10,10,10"/>
        <TextBox TextWrapping="Wrap" Text="{Binding Id, UpdateSourceTrigger=PropertyChanged}" Margin="5,5,250,5"/>
        <Button Content="Add" Margin="555,5,5,5" Command="{Binding AddCommand, Mode=OneWay}"/>
    </Grid>
</UserControl>
